# The icon resource and the READMEs' screenshots are this repository's binary assets, and it is
# worked on with core.autocrlf=true. Git's own heuristic already reads both as binary, but a file
# whose bytes are the artifact would be destroyed by one wrong guess — so it is declared rather
# than detected.
*.ico binary
*.png binary
